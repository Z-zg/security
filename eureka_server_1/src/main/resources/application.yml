server:
  port: 8100
eureka:
  instance:
    hostname: localhost
  client:
    #自己注册自己禁止
    register-with-eureka: false
    #禁止自己检索自己
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:8100/eureka/
